<header>
    <div class="container">
        <img src="{{ 'Luxury-Logo.png' | asset_url }}" width="50px" height="50px">
        <ul class="link-list">
            {% for link in linklists.main-menu.links -%}
                {{ link.title | link_to: link.url }}
            {%- endfor  %}
        </ul>

    </div>
</header>